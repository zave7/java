UserDaoTest 즉 유저서비스에서는 비즈니스로직만을 담당한다고 했는데
여기서 어떤 디비 커넥션을 생성할지를 정하는 기능이 합쳐져있다.
이렇게 된다면 또 다른 디비를 사용하려고하면 아까와 같은 어려움이 뒤따르게 된다
그렇기 때문에 어떤것을 사용할지를 결정하는것은 또다른 기능을 가지는 클래스로 
분리시켜야하겠다

한마디로 비즈니스로직을 담당하는 클래스와 설정을 담당하는 클래스를 나누겠다는 것이다.

그런 클래스를 일단은 DaoFactory라는 이름을 가진 클래스로 정해서 구현했다
그러면 비즈니스로직을 맡은 클래스는 단순히 팩토리라는 클래스의 메소드만 호출하면
될 것이다.

이렇게 성격이 다른 기능들을 그 기능에만 충실한 클래스로 나누어 가면 객체지향을 극대화
시킬수 있게된다

dao팩토리라는 클래스 이름에서 알 수 있듯이 이 클래스는 사용할 dao를 생성하는 역할은 한다.
그러므로 UserDao뿐만 아니라 MemberDao, StudentDao 등등 여러 dao를 불러오게 해야 한다.