1. src에 있는 ojdbc.jar 파일을 받아서 적절한 로컬 경로에 저장
2. 이클립스에서 JDBC를 적용 시키고 싶은 프로젝트 우클릭 -> 맨 아래 properties
3. 		properties 	-> (java build path) 
					-> (library 탭) 
					-> (add external jars..) 
					-> ojdbc 저장했던 경로 선택 후 추가 
					-> 이제 오라클 db와 jdbc로 연동
					
create table usertest(	-- 테스트 테이블
    id varchar2(5) PRIMARY key,
    name varchar2(50) not null,
    password varchar2(50) not null
);