JCF ( Java Collection Framework ) : 크기가 유동적.

	  Collection						Map
추가하거나 삭제는 공통이지만
get은 다르다
List				Set
순서유지O		순서유지X
중복데이터O		중복데이터X(덮어 써버린다)
------------------------------------------------------------------

ArrayList(동기화안된다)		HashSet			HashMap(동기화안된다)		Hashtable(동기화 된다)		
먼저 저장이 끝나는 대로 저장한다
Dto에 넣고 가져오는 작업때문에 거의 순서가 보장된다
물론 벡터보다 빠르다
Vector(동기화 된다)										Properties
벡터는 먼저 호출한 애를 순서대로 저장한다
완벽한 순서가 보장이 된다